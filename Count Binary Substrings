Given a binary string s, return the number of non-empty substrings that have the same number of 0's and 1's, and all the 0's and all the 1's in these substrings are grouped consecutively.

Substrings that occur multiple times are counted the number of times they occur.



/**
 * @param {string} s
 * @return {number}
 */
function countBinarySubstrings(s) {
    let result = 0;
    let prev = 0;
    let curr = 1;
    
    // Count consecutive groups and calculate valid substrings
    for (let i = 1; i < s.length; i++) {
        if (s[i] === s[i - 1]) {
            // Same character, increment current group count
            curr++;
        } else {
            // Different character, we found a boundary
            // Number of valid substrings = min(prev group, curr group)
            result += Math.min(prev, curr);
            prev = curr;
            curr = 1;
        }
    }
    
    // Don't forget the last comparison
    result += Math.min(prev, curr);
    
    return result;
}

// Example usage:
console.log(countBinarySubstrings("00110011")); // Output: 6
// Valid substrings: "0011", "01", "1100", "10", "0011", "01"

console.log(countBinarySubstrings("10101")); // Output: 4
// Valid substrings: "10", "01", "10", "01"

console.log(countBinarySubstrings("00110")); // Output: 3
// Valid substrings: "0011", "01", "10"
